import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Sem configurações especiais — App Router default.
  // Routes definem `runtime = 'nodejs'` individualmente onde precisam de crypto.
}

export default nextConfig
